package wonyong.by.ffmpeg

import android.os.Environment

class Const {
    val STORAGE = Environment.getExternalStorageDirectory().absolutePath + "/StopMotion"
    val RESULT_STORAGE = Environment.getExternalStorageDirectory().absolutePath + "/StopMotionResult"
}